export type TCategory = {
  _id: string;
  name: string;
  imageUrl: string;
  slug?: string;
  description?: string;
  isActive: boolean;
  isDeleted: boolean;
  createdAt: string;
  updatedAt: string;
};
